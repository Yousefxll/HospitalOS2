# خطوات النشر على Render - دليل تفصيلي 🚀

## الخطوة 1: تسجيل الدخول إلى Render

1. اذهب إلى [https://dashboard.render.com](https://dashboard.render.com)
2. سجل الدخول بحساب GitHub (أو أنشئ حساب جديد)
3. اربط حساب GitHub بحساب Render

## الخطوة 2: إنشاء Web Service جديد

1. في Dashboard، اضغط على **"New +"** في أعلى الصفحة
2. اختر **"Web Service"**
3. اختر **"Connect account"** إذا لم تكن متصل بـ GitHub بعد
4. اختر المستودع: **"Yousefxll/HospitalOS2"**

## الخطوة 3: إعداد الخدمة (Service Settings)

املأ البيانات التالية:

### معلومات أساسية:
- **Name**: `hospitalos` (أو أي اسم تفضله)
- **Region**: اختر الأقرب لك (مثلاً: `Singapore` أو `Frankfurt`)
- **Branch**: `main`
- **Root Directory**: اتركه فارغاً (إذا كان المشروع في الجذر)
- **Runtime**: `Node`
- **Build Command**: `yarn install && yarn build`
- **Start Command**: `yarn start`

### خطة الاشتراك:
- اختر **Free** للبداية (يمكن الترقية لاحقاً)

## الخطوة 4: إعداد Environment Variables

في قسم **"Environment Variables"**، اضغط على **"Add Environment Variable"** وأضف المتغيرات التالية:

### متغيرات مطلوبة (Required):

```env
MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
```

**⚠️ مهم**: استبدل:
- `username` - اسم المستخدم في MongoDB Atlas
- `password` - كلمة المرور
- `cluster` - اسم الـ cluster الخاص بك

```env
DB_NAME=hospital_ops
```

```env
JWT_SECRET=your-super-secret-jwt-key-here-change-this
```

**⚠️ مهم**: استخدم مفتاح قوي وعشوائي. يمكنك استخدام:
```bash
openssl rand -base64 32
```

```env
NEXT_PUBLIC_BASE_URL=https://hospitalos.onrender.com
```

**⚠️ مهم**: استبدل `hospitalos` باسم التطبيق الفعلي الذي اخترته في الخطوة 3

### متغيرات اختيارية (Optional):

```env
OPENAI_API_KEY=sk-...
```
(فقط إذا كنت تستخدم ميزات AI)

```env
CRON_SECRET=your-cron-secret-key
```
(سيتم استخدامه للـ cron jobs - يمكنك ترك Render يولد قيمة عشوائية)

```env
NODE_ENV=production
```

## الخطوة 5: إنشاء الخدمة

1. راجع جميع الإعدادات
2. اضغط على **"Create Web Service"**
3. انتظر حتى ينتهي البناء (Build) - قد يستغرق 5-10 دقائق
4. ستظهر لك رسالة **"Live"** عندما يكون التطبيق جاهزاً

## الخطوة 6: تهيئة قاعدة البيانات

بعد اكتمال النشر، يجب تهيئة قاعدة البيانات:

1. افتح المتصفح على: `https://your-app-name.onrender.com/api/init`
   أو استخدم curl:
```bash
curl -X POST https://your-app-name.onrender.com/api/init
```

2. يجب أن ترى رسالة نجاح مثل:
```json
{
  "success": true,
  "message": "Database initialized with default data",
  "credentials": {
    "email": "admin@hospital.com",
    "password": "admin123"
  }
}
```

## الخطوة 7: تسجيل الدخول

1. افتح: `https://your-app-name.onrender.com/login`
2. استخدم:
   - **Email**: `admin@hospital.com`
   - **Password**: `admin123`

## استكشاف الأخطاء

### المشكلة: Build فشل

**الحل**:
- راجع Build Logs في Render Dashboard
- تأكد من أن Build Command صحيح: `yarn install && yarn build`
- تأكد من أن جميع dependencies موجودة في `package.json`

### المشكلة: التطبيق لا يعمل

**الحل**:
1. راجع Runtime Logs في Render Dashboard
2. تأكد من أن جميع Environment Variables موجودة وصحيحة
3. تأكد من أن `MONGO_URL` صحيح ومتصل

### المشكلة: "MONGO_URL is not defined"

**الحل**:
- تأكد من إضافة `MONGO_URL` في Environment Variables
- تأكد من عدم وجود مسافات إضافية في القيمة
- بعد التعديل، اضغط على "Save Changes" وأعد تشغيل الخدمة

### المشكلة: التطبيق بطيء في التحميل الأول

**الحل**:
- هذا طبيعي في خطة Free - التطبيق "ينام" بعد 15 دقيقة من عدم الاستخدام
- التحميل الأول قد يستغرق 30-60 ثانية
- للاستخدام الإنتاجي، فكر في الترقية إلى خطة Paid

## ملاحظات مهمة

### ⚠️ تخزين الملفات (PDF Policies)

المشروع يستخدم filesystem لتخزين ملفات PDF. على Render:
- الملفات قد تُفقد عند إعادة التشغيل (ephemeral filesystem)
- للإنتاج، استخدم S3 أو Cloudinary

### 🔒 الأمان

- لا ترفع ملف `.env.local` إلى Git
- استخدم `JWT_SECRET` قوي وفريد
- HTTPS مفعل تلقائياً على Render

### 📊 MongoDB Atlas

- استخدم MongoDB Atlas للإنتاج (ليس MongoDB محلي)
- تأكد من أن IP Address للخادم مسموح في Network Access
- استخدم Database User مع الصلاحيات المناسبة

---

## ✅ تم! المشروع الآن يعمل على Render

**رابط التطبيق**: `https://your-app-name.onrender.com`

---

**ملاحظة**: إذا كنت تستخدم MongoDB Atlas، تأكد من:
1. إضافة IP `0.0.0.0/0` في Network Access (لجميع IPs)
2. أو إضافة IP الخاص بـ Render (يمكنك العثور عليه في Render Logs)

