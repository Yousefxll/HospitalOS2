# 🔧 حل مشكلة 500 عند حذف Policy

## المشكلة

عند حذف policy، يحدث خطأ 500 Internal Server Error.

## خطوات التشخيص

### 1. تحقق من Logs في Terminal الخاص بـ policy-engine

**الأهم**: يجب أن ترى logs تفصيلية في terminal الذي يعمل فيه policy-engine عند محاولة الحذف.

افتح terminal الذي يعمل فيه `uvicorn` أو `./start.sh` وابحث عن:

```
============================================================
🗑️  DELETING POLICY: 48157ba6-1add-438c-8890-bd1cb0e4a7d9
   Tenant: default
============================================================
```

بعد ذلك يجب أن ترى:
- Step 1: Deleting job files...
- Step 2: Deleting chunks from vector store...
- Step 3: Deleting manifest files...
- Step 4: Deleting policy directory and files...
- Step 5: Verifying deletion...

إذا كان هناك خطأ، سترى:
```
❌ CRITICAL ERROR deleting policy ...: [خطأ هنا]
```

**انسخ الخطأ الكامل وأرسله لي**.

### 2. التحسينات المطبقة

تم تحسين الكود لمعالجة الأخطاء بشكل أفضل:

1. ✅ `load_manifest` الآن تتعامل مع JSON فاسد
2. ✅ `delete_policy_files` لديها معالجة أفضل للأخطاء
3. ✅ Verification step تم تحسينه

### 3. إذا استمر الخطأ

**أعد تشغيل policy-engine** بعد التعديلات:

```bash
cd "/Users/yousef/Downloads/HospitalOS 2/policy-engine"
# أوقف الـ server الحالي (Ctrl+C)
# ثم شغّله مرة أخرى:
./start.sh
```

### 4. تحقق من الأخطاء الشائعة

#### خطأ: "Failed to delete chunks"
- هذا تحذير فقط، لا يمنع الحذف
- ChromaDB قد لا يحتوي على chunks لهذا policy

#### خطأ: "Failed to delete policy files"
- قد يكون هناك ملف قيد الاستخدام
- تحقق من permissions

#### خطأ: "No job files found"
- Policy قد لا يكون لديه job files
- هذا تحذير فقط، لا يمنع الحذف

## الخطوة التالية

**يرجى إرسال logs الكاملة من terminal الخاص بـ policy-engine عند محاولة الحذف**.

